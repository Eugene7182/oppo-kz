[tool.black]
line-length = 100
target-version = ["py311"]
extend-exclude = '''
/(
  .venv
| build
| dist
| frontend/node_modules
)/
'''

[tool.isort]
profile = "black"
line_length = 100
src_paths = ["backend"]
