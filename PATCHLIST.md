
Oppo KZ v4 overlay:
- Import jobs: history list, payload storage, retry API.
- Inventory (stock_balances) + API + UI.
- Improved move recommendations (use balances + safety stock).
- Router/menu updates, Sentry variables example.
Apply: copy over your repo, then `alembic upgrade head`, rebuild frontend.
